# Secrets — never commit
.env
.env.*
.secrets/
*.pem
*.key

# Dependencies and build output
node_modules/
dist/
out/
build/
__pycache__/
*.py[cod]
.venv/
venv/

# Runtime state, caches and artifacts
cache/
data/artifacts/
*.log
.pytest_cache/
.ruff_cache/

# Local scratch — working notes, probe output, one-off dumps
.scratch/
*.tmp

# internal only — never publish
SUBMISSION.md
stability_watch.json
a2a_*.json
user_agent_e2e_results.json
user_agent_e2e_payloads.json
.artifacts/
